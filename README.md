# Heroku_shopping_cart
- First upload files that'll exclude npm module files
- then clone git repository
- then do changes in package.json-> script-> "start":"node file_name/server_file_name"
- deploy
